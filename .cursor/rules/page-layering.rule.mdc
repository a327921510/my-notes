---
description: React 页面四层架构（页面入口 / 区域组件 / 业务 Hooks / 纯展示组件）
globs:
  - "src/pages/**/*.{ts,tsx}"
  - "src/router/routes.tsx"
alwaysApply: false
---

# React 页面四层架构

> 完整代码模板、反模式示例、FAQ：参见 `.cursor/skills/generate-page/references/page-layering-guide.md`。
> 性能相关（memo / useCallback / useMemo）：参见 `react-performance.rule.mdc`。

## 四层职责

| 层 | 文件位置 | 可以做 | 不可以做 |
|---|---|---|---|
| **1. 页面入口** | `pages/<Name>/index.tsx` | 持有跨区域共享状态；编排区域；路由 / 权限判断 | 大段 JSX；可复用的数据操作逻辑 |
| **2. 区域组件** | `pages/<Name>/components/<Region>Panel.tsx` | 区域内部交互（选中、排序、输入）；调用本页业务 Hook | 编排其他区域；直接修改其他区域状态 |
| **3. 业务 Hook** | `pages/<Name>/hooks/use<Domain>.ts` | API 调用、数据状态、缓存、业务规则；返回 `state + actions` | DOM 操作；import React 组件；关心布局 |
| **4. 纯展示组件** | `pages/<Name>/components/<Widget>View.tsx` | 根据 props 渲染；通过 props 回调上报 | API / 数据库；读写全局状态；跨区域编排 |

## 关键约束

1. **纯展示 → 只用 props**：不 import hooks、不访问数据层、必须用 `memo` 包裹。
2. **区域组件 → 对外接口最小化**：只通过 `onXxxChange` / `onRegisterActions` 与入口通信。
3. **业务 Hook → 不感知 UI**：返回 `{ state, actions }`，谁调用由组件层决定。
4. **页面入口 → 薄编排层**：避免在入口写 200+ 行 JSX，复杂 UI 下沉到区域或纯展示。
5. **跨区域联动 → 归入口**：区域 A 影响区域 B，必须通过入口中转，不在 A 中 import B 的内部状态。

## 目录模板

```
src/pages/<PageName>/
├── index.tsx                         # 页面入口
├── types.ts                          # (可选) 页面级共享类型
├── components/
│   ├── <RegionA>Panel.tsx            # 区域组件
│   ├── <RegionB>Panel.tsx
│   ├── <PureWidget>View.tsx          # 纯展示
│   └── ...
└── hooks/
    ├── use<DomainA>.ts               # 业务 Hook
    └── ...
```

## 命名约定

| 类型 | 命名模式 | 示例 |
|------|---------|------|
| 页面入口 | `<PageName>Page` + 默认导出 | `OrderManagementPage` |
| 区域组件 | `<Region>Panel` / `<Region>Section` / `<Region>Sidebar` | `FilterPanel`, `ListSection` |
| 业务 Hook | `use<Domain>` / `use<Domain>Mutations` | `useOrders`, `useOrderMutations` |
| 纯展示 | `<Widget>View` / `<Widget>Card` / `<Widget>Badge` | `OrderDetailView`, `StatusBadge` |

## 演进策略

1. **简单页面**：允许"入口 + 纯展示"两层，业务逻辑可内联在入口。
2. **复杂页面**：抽出顺序——业务 Hooks → 区域组件 → 纯展示。
3. **新建页面**：直接用 `generate-page` skill，按四层骨架生成。

## 高频反模式（命中即重构）

| 反模式 | 修正 |
|-----|------|
| 纯展示组件直接调用 `fetch` / `axios` | 数据操作移到业务 Hook |
| 区域 A 直接 import 区域 B 的 state | 通过页面入口中转 |
| 页面入口 500+ 行 JSX | 拆分区域组件和纯展示 |
| 多个组件各写一套相同的 CRUD | 统一到业务 Hook |
| 为减少 props 透传引入全局 store | 保持 props 传递，或用页面级 Context |
| Hook 内部 `document.querySelector` | DOM 操作放到组件层 ref/effect |
| 纯展示组件没有 `memo` 包裹 | 统一 `memo()` 包裹 |
| 传给 memo 子组件的回调未 `useCallback` | 入口/区域中用 `useCallback` 包裹 |

> 完整反模式分析与代码对照见参考文档。

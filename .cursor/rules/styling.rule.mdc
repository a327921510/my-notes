---
description: Tailwind CSS v4 + Less CSS Modules + Ant Design 主题的样式分工规则
alwaysApply: true
---

# 样式方案规则

项目采用 **Tailwind CSS v4 + Less (CSS Modules) + Ant Design ConfigProvider** 三层样式体系。

## 分工原则

| 场景 | 使用方案 |
|------|---------|
| 布局、间距、flex/grid、响应式、简单装饰 | Tailwind CSS class |
| Ant Design 组件主题色、圆角、字体 | `ConfigProvider` theme token（`src/styles/antd-theme.ts`） |
| Ant Design 组件深度样式覆盖 | Less CSS Modules + `:global(.ant-xxx)` |
| 复杂动画、伪元素、复杂选择器 | Less CSS Modules |
| 全局 CSS 变量 | `src/styles/index.css` 的 `@theme` 块 |

## CSS Modules 约定

- 文件命名：`<ComponentName>.module.less`
- 类名在 Less 中用 camelCase（Vite 自动转换）
- 使用方式：`import styles from './MyComponent.module.less'`
- 覆盖 Ant Design 样式：`:global(.ant-xxx)` 限定在 CSS Modules 的局部类下

```less
// 正确：在局部作用域内覆盖
.tableWrapper {
  :global(.ant-table-thead > tr > th) {
    background: var(--color-brand);
  }
}
```

## 混合使用

Tailwind class 和 CSS Modules class 通过 `clsx` 组合：

```tsx
import styles from "./Card.module.less";
import clsx from "clsx";

<div className={clsx("rounded-lg p-4", styles.card, active && styles.active)} />
```

## 禁止事项

- **禁止**内联 `style={{}}`（除非是运行时动态计算值，如百分比宽度）
- **禁止**使用 `!important`
- **禁止**在 Tailwind 中大量使用 `@apply`（需要抽取样式说明应该用 CSS Modules）
- **禁止**在全局样式中直接写 `.ant-xxx` 覆盖（必须限定在 CSS Modules 的局部类下）
- **禁止**在 Less 文件中硬编码颜色值（使用 `@color-brand` 等 Less 变量或 CSS 变量）

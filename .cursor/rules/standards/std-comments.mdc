---
description: 代码注释规范——AI 生成须写清职责与流程地图（stage），详细且无废话，降低人类阅读成本
alwaysApply: true
---

# 代码注释

项目代码大量由 AI 生成，编写 / 修改代码时**主动补注释**，为人类阅读与后续迭代降本。

**语言**：注释正文用中文；API 名、协议字段、错误码等技术术语保留英文。  
**重心**：写**为什么**与非显而易见处，不复述代码字面；命名已能表达的优先改命名（见 `std-typescript` 数据转换命名）。

## 必须注释

| 对象 | 写什么 |
|------|--------|
| **文件头** | 非平凡模块：一两句职责 + 在链路中的位置 |
| **编排入口** | 流程地图 stage **1→N**（短英文 + 一句中文）；与 `std-readability`「主流程可扫读」一致 |
| **函数 / 流程块** | 业务目的、关键输入输出、副作用；跨层 / 跨端写清数据流向 |
| **非显而易见处** | 协议字段、约定错误码 / 状态码、降级路径、平台限制、magic number —— **为什么** |
| **复杂分支** | 重试、降级、边界与错误处理 —— 意图一句带过 |

## 详细程度

| 场景 | 要求 |
|------|------|
| **公共 / 导出函数** | 函数头：职责 + 非自明的参数 / 返回 / 副作用 |
| **私有小函数、命名已自明** | 可省函数头；非显而易见处再注 |
| **非平凡分支或约 30 行以上** | 块注释标主要步骤 |
| **主流程函数** | `// --- stageName ---` 分段；**stageName 与流程地图同一套**，禁止「解析入参」「处理数据」等泛称 |
| **数据转换函数** | 识别：`map*` / `format*` / `parse*`（命名见 `std-typescript`）。有条件时在函数头写 `例：入参 → 出参` 1～2 组；多分支每类一例。**只写数据形态，不写业务故事** |

## 流程地图（编排入口）

入口文件头或入口函数头列出 stage；函数体内用同名块注释分段。日志 `stage` 字段、E2E 定位属性前缀与 stage **同一套名字**（如 `data-testid="{feature}.{stage}"`）；**只给本条用例会点到的控件打标**，禁止全站扫。不要求为每个 stage 新建函数。

```javascript
/**
 * 提交导出任务（编排入口）
 * 位置：调用方确认参数后 → 创建任务并等待结果
 *
 * 1. validateInput   校验必填与权限
 * 2. createJob       创建远端任务
 * 3. awaitResult     轮询或订阅直至完成/失败
 * 4. resolveOutcome  映射结果码并返回
 */
async function submitExport(params) {
  // --- validateInput ---
  // ...
  // --- createJob ---
  // ...
}
```

## 禁止（无废话红线）

- 不注释自明代码（如 `getUser()` 旁写「获取用户」）。
- 不复述紧邻代码字面（如 `foo.scan()` 旁写「调用 scan」）。
- 不留过时注释；改逻辑时同步更新或删除（AI 易残留）。
- 不保留被注释掉的 dead code；`TODO` 须附说明或追踪。

## 示例

```javascript
// ❌ 废话
const user = getUser(); // 获取用户

// ✅ 约定含义
// code：1=成功 / -1=失败 / 0=用户取消
if (res.code === 0) return;

// ✅ 文件头
/** 外部回调：校验签名 → 落库 → 通知下游；失败进重试队列 */

// ✅ 数据转换（map*）：入参 → 出参，不写业务故事
/**
 * 相对路径补 host；已是 http(s) 则原样。
 * 例：/a/b.html?id=1#foo → https://host/a/b.html?id=1#foo
 * 例：&Id=xxx#/path → https://host/?Id=xxx#/path
 */
```

---
description: 可读性优先——避免过度封装、控制调用深度、主流程可扫读、先早退再计算，可读优先于性能并做好平衡
alwaysApply: true
---

# 可读性优先

代码应能「顺着读下去」。项目代码大量由 AI 生成，生成/修改代码时以**人类可读**为第一目标、性能其次；两者冲突时见「可读优先，性能其次」。

## 避免过度封装
- **未复用不抽离**：逻辑只在一处使用时，写在当前 function / 文件内，不为「架构好看」新建 `utils` / `helpers` / `service`。
- **复用门槛**：至少 **2 处**真实调用，或壳层 / `shared` 已约定的公共能力，才提取。
- **抽离前自问**（三者皆否则不抽离）：跳转后是否更省力？新层是否承载独立职责？拆分后单文件是否仍可读？
- 仅改参数名、或只包一层 Promise 的 wrapper **不算**独立职责。

## 控制调用深度
- 单条业务链路尽量 **≤3 层**（入口 → 编排 → 底层 API / IO）；读者不应为理解一次操作翻 5 个文件。
- 避免 `A → B → C → D` 式链式透传；中间层只做透传时，合并或内联到调用方。

## 主流程可扫读（编排入口 + 流程地图）
用户或调用方可见的一条能力须有**唯一编排入口**：读者先打开这一个文件/函数，应能顺着主路径读完。
- **入口顶部写流程地图**：列出 stage **1→N**（建议 **4～6** 个，超过 **8** 个说明旁路被写进主地图，应砍掉或另开能力）。
- **stage 名**：短英文 camelCase（如 `validateInput`、`applyChange`），与块注释、日志字段、日后测试定位属性前缀**同一套名字**；中文说明写在同一行。
- **分支共用 stage**：不要为每个 if 拆 stage 名（如不要 `createJobA` / `createJobB`）；同一 stage，日志里用 `branch` 区分。
- **旁路不上主地图**：次要分支、可选增强从主路径分出的能力，在地图里最多点一句，细节不进 1→N。
- **非主入口挂载同样适用**：模态、嵌入模块、后台/全局挂载的能力也要有薄编排入口，禁止把过长编排堆进无职责的中间层或巨型共享模块。
- **不为测试硬拆函数**：测试/日志对齐的是 stage 边界，不是「一步一个 export」；未复用的步骤用 `// --- stageName ---` 分段，留在入口内。

## 先早退，再计算（声明靠近使用点）
- **Guard 优先**：前置条件 / 幂等 / 已处理等早退写在函数前部；早退之后再做本路径才需要的计算与副作用。
- **声明靠近使用**：仅在某一分支使用的变量，在该分支内、使用点前声明；禁止在早退检查之前「提前算好」。
- **例外**：多个后续分支共用、或计算本身就是 guard 条件时，可提前声明。

```typescript
// ❌ 早退不用，却提前计算
const returnPath = toRouterRelativePath(pathname, search);
if (oauthStartedRef.current) {
  setUi(loadingUi(…));
  return;
}
startWeChatOAuthRedirect(returnPath);

// ✅ 先早退，再在使用点声明
if (oauthStartedRef.current) {
  setUi(loadingUi(…));
  return;
}
const returnPath = toRouterRelativePath(pathname, search);
startWeChatOAuthRedirect(returnPath);
```

## 何时才拆分
- 同一逻辑已在 **≥2 处**分包 / 模块重复出现。
- 单文件过长（约 **300 行以上**）且能按**独立子流程**切分，切分后每块仍自洽可读。
- 测试 / 构建脚本要求独立模块。
- 不足以拆分时：用 **`// --- stageName ---` 块注释**分段，代替无职责的中间函数。

## 可读优先，性能其次（平衡）
- 默认选**更易读**的写法，即使少量牺牲性能。
- **性能例外**：热路径（hot path）、高频调用、大数据量循环等**确有**性能需求处，可牺牲可读换性能，但必须用注释写明「为什么这样写、优化了什么」。
- 不做没有实测依据的过早优化（premature optimization）。

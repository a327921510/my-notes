---
description: 用户意图自动路由 — 根据请求中的关键词识别任务类型并匹配对应 skill / workflow
alwaysApply: true
---

# 任务自动路由

接到用户请求时，先**静默分类**任务类型，再选择合适的 skill 或 workflow 执行。多步任务在开始前简短说明阶段。

## 关键词 → Skill 映射

| 用户原话中包含 | 任务类型 | 优先采用 |
|---|---|---|
| "新建页面"、"做一个 XX 页"、"仪表盘"、"管理页"、"列表页 + 详情" | 生成页面（四层架构） | `generate-page` skill |
| "对接接口"、"新增 API 模块"、"封装 XX 接口"、"调用后端" | 新建 API 模块 | `add-api-module` skill |
| "全局状态"、"跨页面共享"、"用户信息持久化"、"主题切换" | 新建 Zustand store | `add-zustand-store` skill |
| "新增菜单"、"加路由"、"注册路由"、"新建一个路由项" | 注册路由 | `add-route` skill |

如果同时命中多个，按"页面 → 路由 → store → API"的依赖顺序串联执行（例如"新建一个用户管理页"通常需要：`add-api-module` → `generate-page` → `add-route`）。

## 关键词 → Workflow / Command 映射

| 用户原话中包含 | 任务类型 | 处理方式 / 推荐命令 |
|---|---|---|
| "实现"、"添加"、"做一个" + 功能描述 | 新功能 | 需求确认 → 类型定义 → 编码 → 自检表 → PR |
| "修复"、"bug"、"不工作"、"报错" | Bug 修复 | 复现 → 定位 → 最小修改 → 回归确认 |
| "构建失败"、"编译错误"、"类型错误"、TS 错误码 | 构建错误 | 完整读错误信息 → 定位根因 → 改 → 验证构建 |
| "重构"、"拆一下"、"太大了"、"页面太长" | 重构页面 | 推荐 `/refactor-page` |
| "评审"、"看下质量"、"这段有问题吗"、"review" | 代码评审 | 推荐 `/code-review` |
| "卡顿"、"慢"、"重渲染"、"性能" | 性能体检 | 推荐 `/perf-audit` |
| "键盘用不了"、"无障碍"、"a11y"、"屏幕阅读器"、"焦点丢了" | 可访问性体检 | 推荐 `/a11y-audit` |
| "改样式"、"加暗色"、"主题"、"调间距" | 样式调整 | 优先 Tailwind class → 不够再 CSS Modules → 不直接覆盖 antd 全局样式 |

## 处理原则

1. **简单任务直接执行**，不要为了"显得正式"宣告 5 个阶段。
2. **多文件改动**先列计划再开工，避免半路返工。
3. **跨域任务**（涉及 ≥2 个 skill）在开头一句话说明执行顺序，再串联调用。
4. **不确定意图**时，反问一句澄清后再选 skill；**禁止**猜测后大动干戈。

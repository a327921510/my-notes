{
  "name": "my-notes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @my-notes/web dev",
    "dev:api": "pnpm --filter @my-notes/api dev",
    "build": "pnpm --filter @my-notes/shared build && pnpm --filter @my-notes/api build && pnpm --filter @my-notes/web build",
    "lint": "pnpm --filter @my-notes/web lint"
  },
  "engines": {
    "node": ">=20"
  }
}

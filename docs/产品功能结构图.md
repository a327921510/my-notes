# 产品功能结构图

```
My Notes（V0.7）
├── 1. 本地账户
│   └── 用户信息：导出 / 导入文件管理 JSON（formatVersion 2）+ 冲突向导
│
├── 2. Web / 桌面共用 UI
│   ├── 文件管理（FileManagerPage）★
│   ├── 差异测试（DiffLabPage）— 不落库
│   └── 全局搜索 → fs_files → /files?focus
│
├── 3. 桌面端 Electron 壳
│
└── 4. packages
    ├── @my-notes/shared — createId、text-diff、site-markdown（无 UI）
    └── @my-notes/local-db — NotesDB（含 fs_* 与历史表）
```

已下线并删除页面代码：笔记区、站点信息区、项目文档、项目信息区、云盘。
